{
  "installCommand": "pnpm install",
  "buildCommand": "pnpm --filter @splitter/web build && ls -la dist/",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
