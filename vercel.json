{
  "installCommand": "pnpm install",
  "buildCommand": "pnpm --filter @splitter/web build",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
