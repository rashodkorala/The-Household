{
  "installCommand": "pnpm install",
  "buildCommand": "pnpm --filter @splitter/web build && cp -r apps/web/dist .",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
